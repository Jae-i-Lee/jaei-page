---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { listPublishedPosts } from "@/lib/blog";
import Headline from "@/components/ui/Headline.astro";
import Pagination from "@/components/ui/Pagination.astro";
import PostItem from "@/components/blog/DatabasePostItem.astro";
import type { GetStaticPathsOptions } from "astro";

export async function getStaticPaths({ paginate }: GetStaticPathsOptions) {
  return paginate(await listPublishedPosts("philosophy"), { pageSize: 6 });
}

const { page } = Astro.props;
const philosophy = page.data;

const metadata = {
  title: "Philosophy",
  description: "억지로 뜻을 세우지 않고, 말을 아끼며, 흐르는 대로 바라봅니다.",
};
---

<BaseLayout metadata={metadata}>
  <section class="px-6 py-16 sm:px-8 sm:py-24">
    <div class="mx-auto max-w-3xl">
      <Headline
        tagline="Philosophy"
        title="말 너머의 것들"
        subtitle="억지로 뜻을 세우지 않고, 말을 아끼며, 흐르는 대로 바라봅니다."
        classes={{
          container: "reveal mb-16 border-l border-border pl-6 sm:pl-8",
          title:
            "mb-4 font-sans text-3xl font-bold tracking-tight text-foreground md:text-4xl",
          subtitle:
            "max-w-2xl text-base leading-relaxed text-muted-foreground sm:text-lg",
          tagline: "px-0 py-0 text-xs uppercase tracking-[0.2em]",
        }}
        titleAs="h1"
      />

      <div>
        {philosophy.map((post) => <PostItem post={post} />)}
      </div>
      <Pagination page={page} />
    </div>
  </section>
</BaseLayout>
